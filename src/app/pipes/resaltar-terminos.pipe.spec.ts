import { ResaltarTerminosPipe } from './resaltar-terminos.pipe';

describe('ResaltarTerminosPipe', () => {
  it('create an instance', () => {
    const pipe = new ResaltarTerminosPipe();
    expect(pipe).toBeTruthy();
  });
});
