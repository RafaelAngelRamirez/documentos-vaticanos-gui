import { TieneElPermisoPipe } from './tiene-el-permiso.pipe';

describe('TieneElPermisoPipe', () => {
  it('create an instance', () => {
    const pipe = new TieneElPermisoPipe();
    expect(pipe).toBeTruthy();
  });
});
